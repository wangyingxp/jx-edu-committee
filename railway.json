{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "npm install && node seed.js"
  },
  "deploy": {
    "startCommand": "node server.js",
    "restartPolicyType": "ON_FAILURE",
    "numReplicas": 1
  }
}
